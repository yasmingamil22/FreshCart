import { CustomCurrencyEgpPipe } from './custom-currency-egp.pipe';

describe('CustomCurrencyEgpPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomCurrencyEgpPipe();
    expect(pipe).toBeTruthy();
  });
});
