import { TextCuttingPipe } from './text-cutting.pipe';

describe('TextCuttingPipe', () => {
  it('create an instance', () => {
    const pipe = new TextCuttingPipe();
    expect(pipe).toBeTruthy();
  });
});
